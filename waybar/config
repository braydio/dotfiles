{
  "position": "top",
  "height": 36,
  "style": "style.css",

  "modules-left": [
    "group/workspaces-title"
  ],
  "modules-center": [
    "custom/center_l",
    "clock",
    "custom/center_r"
  ],
  "modules-right": [
    "group/media",
    "group/system",
    "tray"
  ],

  "group/workspaces-title": {
    "orientation": "inherit",
    "modules": [
      "custom/launcher",
      "hyprland/workspaces",
      "hyprland/window"
    ]
  },
  "group/media": {
    "orientation": "inherit",
    "modules": [
      "mpris"
    ]
  },
  "group/system": {
    "orientation": "inherit",
    "modules": [
      "network",
      "pulseaudio",
      "bluetooth",
      "idle_inhibitor"
    ]
  },

  "custom/launcher": {
    "format": "󰀻",
    "tooltip": "Launcher",
    "on-click": "sh -lc 'command -v wofi >/dev/null 2>&1 && wofi --show drun || (command -v fuzzel >/dev/null 2>&1 && fuzzel --show drun) || (command -v rofi >/dev/null 2>&1 && rofi -show drun) || (command -v thunar >/dev/null 2>&1 && thunar) || kitty'"
  },

  "custom/center_l": { "format": "", "tooltip": "" },
  "custom/center_r": { "format": "", "tooltip": "" },

  "hyprland/workspaces": {
    "disable-scroll": false,
    "enable-shortnames": true,
    "strip-wl": true
  },

  "hyprland/window": {
    "max-length": 60,
    "separate-outputs": false
  },

  "mpris": {
    "format": "{title}",
    "format-paused": " {title}",
    "max-length": 32,
    "tooltip-format": "{player} — {title}"
  },

  "clock": {
    "format": "{:%a %b %d  %I:%M %p}",
    "format-alt": "{:%Y-%m-%d  %H:%M}",
    "tooltip": "{:%A, %B %d, %Y\n%c}"
  },

  "network": {
    "format": "󰤨 {essid}",
    "format-disconnected": "󰤭 offline",
    "format-alt": "⇣ {bandwidthDownBits} ⇡ {bandwidthUpBits}",
    "tooltip": "Network status"
  },

  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-muted": "󰝟 mute",
    "format-icons": ["󰕿", "󰖀", "󰕾"],
    "tooltip": "Audio volume",
    "on-click": "sh -lc 'pactl set-sink-mute @DEFAULT_SINK@ toggle'",
    "on-scroll-up": "sh -lc 'pactl set-sink-volume @DEFAULT_SINK@ +5%'",
    "on-scroll-down": "sh -lc 'pactl set-sink-volume @DEFAULT_SINK@ -5%'"
  },

  "bluetooth": {
    "format": "󰂯 {status}",
    "format-connected": "󰂯 {num_connected}",
    "tooltip-format": "Bluetooth: {status}",
    "on-click": "sh -lc 'if bluetoothctl show | grep -q \"Powered: yes\"; then bluetoothctl power off; else bluetoothctl power on; fi'"
  },

  "idle_inhibitor": {
    "format": "{icon}",
    "format-icons": {
      "activated": "",
      "deactivated": ""
    },
    "tooltip": "Caffeinate: prevent sleep when ON"
  },

  "backlight": {
    "format": " {percent}%",
    "on-scroll-up": "sh -lc 'brightnessctl set +5%'",
    "on-scroll-down": "sh -lc 'brightnessctl set 5%-'"
  },

  "battery": {
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "states": {
      "warning": 25,
      "critical": 10
    },
    "format-icons": [
      "", "", "", "", ""
    ]
  },

  "tray": { "spacing": 6 }
}
